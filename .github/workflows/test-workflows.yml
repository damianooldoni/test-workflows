on:
  push:
    branches:
      - 'main'


name: test-workflows

jobs:
  test-workflows:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2.3.4
        with:
          fetch-depth: 0
